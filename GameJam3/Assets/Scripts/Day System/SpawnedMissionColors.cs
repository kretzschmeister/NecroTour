using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnedMissionColors : MonoBehaviour
{
    public Color spawnedNormalColor, spawnedHighlightedColor, spawnedPressedColor;
    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
